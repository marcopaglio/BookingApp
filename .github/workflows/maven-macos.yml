# This workflow will build a Java project with Maven
name: Java CI with Maven in MacOS

on:
  push:
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: macos-12
    env:
      workdir: booking-aggregator
    
    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '11'

    - name: Install X11 dependencies
      run: |
        which Xvfb
        where Xvfb
        /opt/X11/bin/Xvfb :99 -ac -screen 0 1024x768x24 &
        export DISPLAY=:99
        echo "DISPLAY=$DISPLAY" >> "$GITHUB_ENV"
#        plutil -p ~/Library/Preferences/com.apple.spaces.plist
#        brew install --cask xquartz
#        sudo shutdown -r now
#        open -a XQuartz
#        plutil -p ~/Library/Preferences/org.macosforge.xquartz.X11.plist
#        ip=$(ifconfig en0 | grep inet | awk '$1=="inet" {print $2}')
#        display_number=$(ps -ef | grep "Xquartz :\d" | grep -v xinit | awk '{ print $9; }')
#        export DISPLAY=$ip:$display_number
#        /opt/X11/bin/xhost +$ip
#        echo "DISPLAY=$DISPLAY" >> "$GITHUB_ENV"

    - name: Install Docker
      uses: docker-practice/actions-setup-docker@master

    - name: Cache Maven packages
      uses: actions/cache@v3
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml', '**/*.yml') }}
        restore-keys: ${{ runner.os }}-m2-

    - name: Build with Maven Wrapper
      id: maven-build
      run: ../mvnw install -Pdocker -Ddisplay=$DISPLAY
      working-directory: ${{ env.workdir }}
